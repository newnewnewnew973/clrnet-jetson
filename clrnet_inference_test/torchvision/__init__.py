"""Minimal torchvision shim for CLRNet CULane dataset imports.

The official CLRNet dataset module imports torchvision, but the CULane
inference/evaluation path used here does not call any torchvision API.
"""
